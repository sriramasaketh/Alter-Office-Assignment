import React from 'react'
import ReactDOM from 'react-dom/client'
import IntroductionToReactJS from './Introduction-to-React-JS'

const root = ReactDOM.createRoot(document.getElementById('root'))
root.render(
  <React.StrictMode>
    <IntroductionToReactJS />
  </React.StrictMode>,
)
